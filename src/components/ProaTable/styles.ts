import { TableCell } from '@mui/material';
import { styled } from 'styled-components';
import { theme } from 'styles/global';

export const TableCellHead = styled(TableCell)`
    color: ${theme.colors.composition.white};
    font-size: 16px;
`;
